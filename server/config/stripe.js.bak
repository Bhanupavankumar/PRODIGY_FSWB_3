import Stripe from 'stripe';  // import the Stripe class

const stripeInstance = new Stripe(process.env.STRIPE_SECRET_KEY); // initialize with your secret key

export default stripeInstance; // export the instance
